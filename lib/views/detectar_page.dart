import 'package:flutter/material.dart';

class DetectarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Página de Detectar'),
      ),
      body: Center(
        child: Text('Contenido de la página de Detectar'),
      ),
    );
  }
}
